Removes every appearance from the currently active display state.
In a part this clears face, feature, body and part appearances, including any applied by hand.
In an assembly only component appearances are cleared; the referenced part files are never modified.
Other display states are not affected. Asks for confirmation before running.
